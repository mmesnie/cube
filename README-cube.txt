-InitGraph() must include the directory where the .bgi
 files are (e.g., c:\windows\desktop\cube).
-Options --> Directories --> Unit directories must
 point to the directory with graph.tpu (e.g., c:\windows\tpc\units) 